Coloca aqui estos archivos:

- `logo.png`: el logo de tu servidor.
- `background.mp4`: el video de fondo en bucle para la portada.

La pagina ya apunta a esos nombres, asi que cuando los agregues se van a mostrar automaticamente.
